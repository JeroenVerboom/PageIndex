{
    // ═══════════════════════════════════════════════════════════════════════
    // VS Code Debug Configuration for PageIndex
    // ═══════════════════════════════════════════════════════════════════════
    "version": "0.2.0",
    "configurations": [
        {
            // ─────────────────────────────────────────────────────────────────
            // PageIndex Starter Script
            // ─────────────────────────────────────────────────────────────────
            // Runs the starter script that:
            //   1. Converts DOCX to Markdown
            //   2. Generates PageIndex tree structure
            //   3. Allows interactive document querying
            // ─────────────────────────────────────────────────────────────────
            "name": "🚀 Run Starter Script",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/starter_script.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "python": "${workspaceFolder}/.venv/bin/python",
            "justMyCode": true
        },
        {
            // ─────────────────────────────────────────────────────────────────
            // Run PageIndex on PDF
            // ─────────────────────────────────────────────────────────────────
            // Original PageIndex script to generate tree from PDF
            // Usage: Set the PDF path in "args" below
            // ─────────────────────────────────────────────────────────────────
            "name": "📄 Run PageIndex (PDF)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/run_pageindex.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "args": [
                "--pdf_path",
                "${workspaceFolder}/your_document.pdf"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "python": "${workspaceFolder}/.venv/bin/python",
            "justMyCode": true
        },
        {
            // ─────────────────────────────────────────────────────────────────
            // Run PageIndex on Markdown
            // ─────────────────────────────────────────────────────────────────
            // Original PageIndex script to generate tree from Markdown
            // Usage: Set the Markdown path in "args" below
            // ─────────────────────────────────────────────────────────────────
            "name": "📝 Run PageIndex (Markdown)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/run_pageindex.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "args": [
                "--md_path",
                "${workspaceFolder}/your_document.md"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "python": "${workspaceFolder}/.venv/bin/python",
            "justMyCode": true
        },
        {
            // ─────────────────────────────────────────────────────────────────
            // Debug Current File
            // ─────────────────────────────────────────────────────────────────
            // Runs whichever Python file you currently have open
            // ─────────────────────────────────────────────────────────────────
            "name": "🐍 Debug Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "python": "${workspaceFolder}/.venv/bin/python",
            "justMyCode": true
        }
    ]
}